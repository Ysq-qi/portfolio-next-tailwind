"use client";

import React from "react";

const Orders: React.FC = () => {
  return (
    <main className="flex flex-col items-center">
      <h2 className="text-xl font-bold mb-4">訂單查詢</h2>
      <p>這裡是「訂單查詢」的預設內容。可放置訂單列表、篩選等。</p>
    </main>
  );
};

export default Orders;
